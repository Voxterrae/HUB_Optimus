# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: hub-optimus
metadata:
  template: hub-optimus-agent@0.0.1-beta

services:
  agent:
    project: ./agent
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
