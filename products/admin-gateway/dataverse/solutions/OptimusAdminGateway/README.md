# OptimusAdminGateway empty Dataverse baseline

This directory contains the sanitized, unpacked source of the empty unmanaged
`OptimusAdminGateway` v0.1.0.0 solution.

The owner created and exported the baseline from a private non-production
client-zero sandbox after confirming:

- publisher unique name `HUB_Optimus`;
- customization prefix `opt`;
- choice value prefix `88483`;
- zero root components;
- zero missing dependencies.

The source export SHA-256 was:

```text
f39bf91d8e15d723c5b2cedfa803602cb529f241171823ad8c27c28441e928e9
```

The binary export, tenant and environment identifiers, user identities, URLs
and operational evidence are deliberately not committed. This baseline is not
a release, deployment attestation or production certification.
